# Stage 설정

- 대상: 미설정
- 허용 범위: 미설정
- 최종 목표: 미설정

이 파일은 문제별 메모 공간이다. 대상·범위·목표는 Claude를 시작한 뒤 첫 메시지에도 명시한다.

`MAP.md`, `LEDGER.md`, `EVENTS.jsonl`, `DECISIONS.jsonl`, `runtime/`, `evidence/`는 상위 문제 폴더에서 Stage 1–3이 함께 이어 쓴다. 이 폴더에는 이 Stage에서만 새로 만든 작업 파일과 메모를 둔다.
